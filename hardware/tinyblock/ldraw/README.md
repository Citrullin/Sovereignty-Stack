# Tinyblock LDraw files

This is a collection of some LDraw files, mostly for the bit.block sets.
Or at least somewhat compatible with it. The Lighthouse, for example,
has no bit.block feature yet. Which could be an interesting iteration.

